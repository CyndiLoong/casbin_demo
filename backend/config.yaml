server:
  port: 8080
  mode: debug

database:
  host: localhost
  port: 5432
  user: postgres
  password: postgres
  dbname: casbin_demo
  sslmode: disable
  timezone: Asia/Shanghai

redis:
  host: localhost
  port: 6379
  password: ""
  db: 0
  pool_size: 10

jwt:
  secret: casbin-demo-jwt-secret-key-change-in-production
  expire_hours: 24
  issuer: casbin-demo

rabbitmq:
  host: localhost
  port: 5672
  user: guest
  password: guest
  vhost: /
